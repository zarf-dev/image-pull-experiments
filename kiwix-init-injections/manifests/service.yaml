apiVersion: v1
kind: Service
metadata:
  name: kiwix-serve
  namespace: kiwix-init
  labels:
    app: kiwix-serve
    zarf.dev/connect-name: kiwix
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: kiwix-serve